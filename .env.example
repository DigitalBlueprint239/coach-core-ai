# =============================================================
# Coach Core AI - Environment Configuration Template
# -------------------------------------------------------------
# Copy this file to `.env.local` for local development or
# `.env.production` for deployments. NEVER commit files that
# contain real secrets.
# =============================================================

# -------------------------------------------------------------
# Firebase Configuration (REQUIRED)
# Obtain these values from the Firebase console under
# Project Settings > General.
# -------------------------------------------------------------
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
# Optional: Only required if analytics is enabled in Firebase
VITE_FIREBASE_MEASUREMENT_ID=

# -------------------------------------------------------------
# Core Application Settings
# -------------------------------------------------------------
# Base URL for backend API calls. Use your local API gateway
# during development or the production API domain in prod builds.
VITE_API_BASE_URL=http://localhost:3000
# Environment labels for runtime toggles
VITE_APP_ENV=development
NODE_ENV=development

# -------------------------------------------------------------
# Stripe Configuration (OPTIONAL)
# Required only if payments/subscriptions are enabled.
# Publishable keys are safe for the browser, secret keys should
# remain server-side only.
# -------------------------------------------------------------
VITE_STRIPE_PUBLISHABLE_KEY=
# Server-side only — do not expose in the browser bundle
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# -------------------------------------------------------------
# AI / LLM Integrations (SERVER-SIDE ONLY)
# Keep these values outside of the browser bundle. Use a secure
# server proxy or infrastructure secrets store.
# -------------------------------------------------------------
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# -------------------------------------------------------------
# Analytics & Monitoring (OPTIONAL)
# -------------------------------------------------------------
VITE_GA_MEASUREMENT_ID=
VITE_SENTRY_DSN=

# -------------------------------------------------------------
# Feature Flags & Optional Toggles
# -------------------------------------------------------------
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_ERROR_REPORTING=false
VITE_ENABLE_STRIPE=false
VITE_ENABLE_PWA=false

# -------------------------------------------------------------
# Additional Notes
# -------------------------------------------------------------
# - Populate `.env.local` for local development overrides.
# - For production builds, create `.env.production` and store it
#   securely (e.g., in your CI/CD platform's secrets manager).
# - After editing environment variables, restart the dev server.
# - Never check real secrets into version control.
